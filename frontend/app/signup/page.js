import SignupPageContainer from "@/containers/signuppage-container";
import styles from "./style.module.scss";

const Signup = () => {
  return (
    <main className={styles.signup}>
      <SignupPageContainer />
    </main>
  );
};

export default Signup;
